<div class="d-flex flex-row justify-content-between p-3">
<%= link_to '<i class="fas fa-bars fa-2x text-white"></i>'.html_safe, home_main_path %>
<h2 class="text-white text-center">My Expenses</h2>
<%
=begin%>
 <%= link_to '<i class="fas fa-sign-out-alt fa-2x text-white"></i>'.html_safe, destroy_employee_session_path, method: :delete %> 
<%
=end%>
</div>
<div class="d-flex flex-row justify-content-between p-3">
  
  <span class="text-white push-b">Most recent</span>
  <span class="text-muted push-b">Most ancient</span>
</div>

<div class="g-bg">
  
  <div class="p-3">
    <% @employee_expenses.each do |expense| %>
     
        
        <div class="media-body">
          <div class="float-left">
          <h6 class="text-muted"><%= expense.name %></h6>
         
          <span class="text-muted l-h"><%= expense.created_at.strftime("%d-%b-%Y") %></span>
          </div>
          
        </div>
      </a>
    <% end %>
  </div>
<div>
<div class="g-bg">
  <div class="total-div border-bottom border-muted bg-light">
    <span class="p-3 float-left text-muted">Total</span><span class="p-3 float-right font-weight-bold"></span>
  </div>
  <div class="p-3">
    
  </div>
<div>
<div class="create-button-area text-center">
  <%= button_to 'Create New',new_expense_path,class:"create-button border border-white text-white btn btn-secondary" %>
</div>